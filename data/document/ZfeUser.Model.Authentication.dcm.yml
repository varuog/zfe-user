## YAML Template.
ZfeUser\Model\Authentication:
  type: embeddedDocument    
  fields:
    authToken:
        unique:
            order: desc
        type: string
    authTokenTime:
        unique:
            order: desc
        type: int  
    logIp:
        unique:
            order: desc
        type: string      
    deviceID:
        type: string     