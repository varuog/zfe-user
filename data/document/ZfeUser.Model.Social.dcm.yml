## YAML Template.
ZfeUser\Model\Social:
  type: embeddedDocument
  fields:
    id:
        id: true
        strategy: none
        type: int   
    accessToken:
        type: string
    providerName:
        type: string
  indexes:
      socialAccessToken:
        keys:
            accessToken: desc
            providerName: desc
        options:
            sparse: true
            unique: true