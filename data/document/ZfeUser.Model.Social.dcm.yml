## YAML Template.
ZfeUser\Model\Social:
  type: embeddedDocument
  fields:
    id:
        id: true
        strategy: none
        type: int   
    accessToken:
        type: string
    accessTokenSecret:
        type: string
    providerName:
        type: string
  indexes:
      providerIndex:
        keys:
            providerName: desc
        options:
            sparse: true
            unique: true
      accessTokenIndex:
        keys:
            accessToken: desc
        options:
            sparse: true
            unique: true